//create an array for stop light color

colur = ["red", "yellow", "green"]

function setup() {

    createCanvas(400, 400);
    background(200);
    fill('red');
    ellipse(200, 200, 200, 200)
}

// to set color
function setColor(s) {
    for(var i= 0; i< 5; i++){
        fill
    }
    console.log("red");
    
}

