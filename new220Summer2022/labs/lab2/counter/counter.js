let grades = 0;
// draw is a funciton within JS and within p5. This allows for drawing with the plugins

//function to console log grades
function draw() {
    grades = grades +1; // this adds 1 every console log.
    console.log (grades);

}